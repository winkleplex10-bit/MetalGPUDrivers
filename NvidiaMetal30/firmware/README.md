GSP firmware is not used by the GOP framebuffer kext.

When RM bring-up starts, pair blobs with open-gpu-kernel-modules **610.57.04** (NVIDIA `.run --no-kernel-modules`). Do not commit firmware here.

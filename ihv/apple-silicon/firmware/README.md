# Firmware — Apple Silicon track

Blobs are **not** committed unless a macOS redistribution grant is documented.

Vendor firmware bring-up notes live with the vendor IHV (`ihv/nvidia/firmware`, `ihv/amd-*/firmware`, …). This directory holds AS-specific load/path notes only.

**WhateverGreen:** absent. Do not use WEG to “enable” firmware or personalities.
